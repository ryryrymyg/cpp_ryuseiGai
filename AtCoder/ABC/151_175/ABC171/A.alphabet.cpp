#include <bits/stdc++.h>

using namespace std;

#define rep(i, n) for(int i = 0; i < (n); i++)
typedef pair<int, int> P;

int main(){
  char A;
  cin >> A;

  if('A' <= A && A <= 'Z'){
    cout << 'A' << endl;
  }
  else{
    cout << 'a' << endl;
  }
}
